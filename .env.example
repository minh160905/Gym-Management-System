# Copy file này thành .env và điền thông tin kết nối database của bạn
# cp .env.example .env

# Chuỗi kết nối PostgreSQL
# Có thể dùng Neon (https://neon.tech), Supabase, Railway, hoặc PostgreSQL local
DATABASE_URL=postgresql://<USER>:<PASSWORD>@<HOST>/<DATABASE>?sslmode=require
